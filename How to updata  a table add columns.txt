ALTER TABLE  HMDA..[STAGE_HEALTH]
ADD  STATE_CENSUS_GEO_ID VARCHAR(100),
     COUNTY_CENSUS_GEO_ID VARCHAR(100),
     ZIP_CODE_CENSUS_GEO_ID VARCHAR(100),
     PLACE_CENSUS_GEO_ID VARCHAR(100),
	 TRACT_CENSUS_GEO_ID VARCHAR(100)

这个表格是在HMDA database下的 Add后面无需加单词column， 假如是drop 的话， 需要是drop column 【column name】